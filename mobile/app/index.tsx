// mobile/app/index.tsx
// Entry point — Expo Router loads this as the default screen (/)
// We simply render our SearchScreen here.
import SearchScreen from "./search";

export default SearchScreen;
